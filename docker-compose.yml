services:
  weanalytics:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    environment:
      TRUST_PROXY: "1"
    volumes:
      - weanalytics-data:/app/backend/data
    restart: unless-stopped

volumes:
  weanalytics-data:
