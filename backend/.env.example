PORT=3000
JWT_SECRET=change-me-long-random-string
REGISTER_SECRET=change-me-invite-bootstrap-code
TRUST_PROXY=0
# Production behind HTTPS: set COOKIE_SECURE=1 so auth cookies are sent only over TLS
COOKIE_SECURE=0

# Public URL for EasyDonate callback (stable after deploy; no trailing slash)
PUBLIC_URL=http://2.26.51.242:3000

# Donate webhook: POST /api/donate/callback?token=<webhook_secret from dashboard>
# or header X-Webhook-Secret (EasyDonate uses the URL with ?token=)
